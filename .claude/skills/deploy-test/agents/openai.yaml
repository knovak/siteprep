interface:
  display_name: "Deploy Test"
  short_description: "Refresh an initiative's test deployment"
  default_prompt: "Use $deploy-test to refresh this initiative's test deployment and report both environment URLs."
