experiment with wasm — Demo prompt history
2026-09-07

Create an initiative, 'experiment with wasm'.  the wish is to convert at least one functional web site and web UI with a wasm-based non-server application.

Evaluate the bookmark-sorter and the tide-here applications to choose the best fit for the conversion.  Then fork it into this initiative and write the application so that it runs alone without a backend server and with a wasm module running in the browser performing the same functions.

---

wow, will this work forever?  or will it lose its database at some point?

---

can you do the same for tide-here?

---

i wonder if these should work as well on an ipad under safari?

---

great. please extend PR 467 to include the wish, and the findings you've discovered and the web pages you've created with the sample applications.

I'd like this to deploy as a static website under demos/ .  it's first page should have a brief explanation of wasm and then links to these first two applications.

2026-09-08
enable as much internet access as possible for both applications.  make a PR
