# Message audience review — September 12, 2026 (UTC)

This is the first Phase 4 increment, covering recipient resolution only. The
`build-message-handoff` item remains open for approval, exact prompt export,
protected link material, optional discussion posting and outcome reporting.

- 55 real-D1/domain/HTTP/time tests pass, including ten new audience cases.
- Six audience interface journeys pass across Chromium, Firefox and WebKit
  at desktop/phone sizes; `audience-20260912.json` records versions and checks.
- Six existing coordination journeys are recorded separately in
  `audience-coordination-regressions-20260912.json`. The earlier 60 gathering/
  access journeys are retained as Phase 3 evidence, not claimed as new runs.
- TypeScript and lint pass; build, scope and compiled-Worker checks are reported
  with the PR. The changed source hashes are in `audience-source-20260912.json`.

The tests cover all three groups, channel restrictions without substitution,
incomplete profiles, shared destinations, individual selection and foreign-ID
rejection; unanswered invitations and polls with reviewed subsets, latest votes
and invitation generations; and balances before/after payment confirmation.
An activity-specific payment audience excludes obligations from another activity.

Authority tests reject members, previews, other-fling organizers, closed flings,
stale revisions, missing origin/CSRF and a changed expected organizer. The
snapshot creates no profile, code, session, audit or approval record. The browser
also verifies keyboard review, phone layout and clearing a result after a newer
profile revision is observed. No code, contact or cookie is written to the
machine-readable browser receipts.

Audience rows are a transient preview. They do not approve content, create
stable handoff delivery IDs, acquire member codes, dispatch a message or prove
receipt. This completes only the audience portion of T8; later T8–T10, recovery,
hosted identity, dependency remediation and independent pilot evidence retain
the plan's existing boundaries. Flings still has no deployment target or waiting
deploy-record branch, so the test-deployment phase skips it.
