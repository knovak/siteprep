# Optional discussion posting — September 14, 2026 (UTC)

This Phase 4 increment approves one optional discussion post with separately
reviewed shared text and readers. The selected `build-message-handoff` item
remains actionable for selected retries, account-history inspection and attempt
tracking. No real external message, recipient receipt or pilot acceptance is
established by this fictional rehearsal.

## Validation

- `npm test`: **96** real-D1/domain/HTTP/time tests pass, including **nine** new
  discussion cases. Each database applies all eight generated migrations.
- `node test/message-discussions-browser.mjs`: **six** journeys pass across
  Chromium, Firefox and WebKit at 1280×1000 and 390×844.
- Existing exact-message, result-reporting and coordination browser suites:
  **18** regression journeys pass across the same engines and sizes. This run
  covers **24** browser journeys, not every historical fixture suite.
- TypeScript and lint pass. Application/repository builds, scope checks and
  current GitHub status are reported in the PR and deployment receipt.
- The adjacent source receipt records the refreshed-main baseline, runtime and
  hashes of the changed application, migration and test files. Browser receipts
  contain no bearer links, contacts, cookies or exported prompts. Authenticated
  screenshot-only state remains under ignored owner-only `.wrangler/qa/`.

Database cases cover independent whole-fling/activity/event readers; malformed,
foreign and mismatched scopes; personal-link rejection; owner/member/preview
permissions; fingerprint tampering; required separate discussion consent;
concurrent approval; approval invalidated by changed readers or late closure;
and an injected final failure rolling back approval, post, link, revision and
audit. Recopy succeeds after the transaction's own revision advance and does
not duplicate the post. Other prepared batches become stale.

Current reported counts use each delivery's latest report, preserving unknowns
for missing rows and allowing corrections. Reports after link expiry use no raw
payload. Member projections contain no notification batch ID, private suffix,
contact or claimed evidence. Posts use identifiers independent of batch IDs.
Editing and hiding preserve approved text and apply existing member redaction.

The browser journeys exercise independent text/audience review, the additional
approval confirmation, keyboard action, one post after copy/recopy, reported
counts after reload and the organizer link opening the correct review history.
No horizontal overflow or page errors occur. Clipboard access is stubbed. The
new test was corrected to target the outer history disclosure and to avoid
assuming reader name order; IDs determine the stable review order.

## Limits

The optional-post portion of T10 has local simulated evidence. Explicit
selected retries are still required for full T9 and Phase 4. The host remains
a private fictional test environment with its existing visitor-bound rehearsal
identity; independent organizer identity, recovery, development-toolchain
remediation and human pilot findings remain later work. No provider adapter,
external LLM operation, real message or production release is introduced.
