# Selected retry evidence — September 14, 2026

This receipt covers the final local Phase 4 increment with fictional records,
simulated account-history observations and a stubbed browser clipboard.
Nothing operates Gmail, Messages or an external sending tool.

- **107** real-D1/domain/HTTP/time tests passed. The **11** new retry tests cover
  explicit selection/history acknowledgements, exact selected content and stable
  IDs, original-owner authority, sent/suppressed exclusion, concurrent exports,
  injected rollback, reporting/export races, attempt-specific reporting, expiry,
  revocation, context changes, one linked post, redaction and HTTP boundaries.
- After the final status-label/count adjustment, all **20** retry and discussion
  tests passed again. TypeScript and lint passed; the application build passed.
- **24** browser journeys passed across Chromium, Firefox and WebKit at
  1280×1000 and 390×844: six new retry journeys and six each for existing exact
  messages, reporting and discussion posting. Receipts are the adjacent
  `message-retries-*-20260914.json` files. They record browser versions, viewport
  and explicit checks without bearer links or session values.
- The retry journeys select one delivery, require history/run acknowledgements,
  review using the keyboard, discard review after editing an observation, copy
  only the exact selected delivery, preserve one discussion post, record an
  attempt-2 report, clear the old displayed prompt, disallow retry of reported
  sent, and retain attributed history after reload. No horizontal overflow or
  page error was observed.

These simulated T9 checks join the prior T8 exact-message and T10 discussion
receipts to complete local Phase 4 implementation. Human account inspection,
correct live sender setup, recipient receipt, external duplicate prevention and
representative pilot acceptance remain unproven. Phase 5 editable recovery and
later hosted/identity work remain on the implementation plan.

The additive `0008` migration preserves existing result rows as attempt 1. The
only reconstructible message links remain in the original expiring encrypted
payload. Retry history stores selected IDs, hashes, times and claimed inspection
evidence; it neither duplicates personal links nor rewrites approved messages.
